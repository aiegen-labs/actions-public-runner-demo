name: TEMPORARY control - same file on a hosted runner

on:
  workflow_dispatch:

jobs:
  should-never-run:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "control: this workflow parses and dispatches fine"
