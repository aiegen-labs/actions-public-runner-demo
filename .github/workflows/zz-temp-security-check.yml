name: TEMPORARY security check - public repo must not reach production runners

on:
  workflow_dispatch:

jobs:
  should-never-run:
    runs-on:
      group: aiegen-home-production-release
      labels: [self-hosted, linux, x64, aiegen-home, n100, aiegen-home-builder]
    timeout-minutes: 2
    steps:
      - run: echo "FAIL: a public repository reached a production runner"
