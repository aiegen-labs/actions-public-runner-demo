# Actions Public Runner Demo

Minimal public repository used to verify that the `aiegen-labs` organization can allocate standard GitHub-hosted runners.

The regression workflow runs a small contract on Linux, macOS, and Windows. It intentionally avoids secrets, deployments, and billable larger runners.

